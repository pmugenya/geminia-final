import { Routes } from '@angular/router';
import { QuoteUsersComponent } from './quote-users.component';

export const quoteUsersRoutes: Routes = [
    {
        path: '',
        component: QuoteUsersComponent
    }
];
